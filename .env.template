# ============================================================================
# APEX WEALTH ADVISOR - ENVIRONMENT CONFIGURATION
# ============================================================================
# Copy this to .env and fill in your actual values.
#
# Architecture:
# - Internal MCP (Portfolio Data): Okta XAA (ID-JAG token exchange)
# - Google Calendar: Auth0 Token Vault
# - Salesforce CRM: Auth0 Token Vault (Phase 2)
# ============================================================================

# ============================================================================
# SECTION 1: CLAUDE AI
# ============================================================================
# Claude API for the AI assistant

ANTHROPIC_API_KEY=your-anthropic-api-key-here
CLAUDE_MODEL=claude-sonnet-4-20250514

# ============================================================================
# SECTION 2: OKTA - Identity Provider (SSO)
# ============================================================================
# Okta organization for user authentication

# Your Okta domain (e.g., dev-12345.okta.com)
OKTA_DOMAIN=your-okta-domain.okta.com

# OAuth client for frontend SSO
OKTA_SSO_CLIENT_ID=your-sso-client-id
OKTA_SSO_CLIENT_SECRET=your-sso-client-secret

# Issuer URL for token validation
OKTA_ISSUER=https://your-okta-domain.okta.com/oauth2/default

# ============================================================================
# SECTION 3: OKTA XAA - Cross-App Access (Internal MCP)
# ============================================================================
# ID-JAG token exchange for accessing Internal MCP server
# This enables: ID Token -> ID-JAG Token -> MCP Access Token

# Authorization server ID for MCP resources
OKTA_MCP_AUTH_SERVER_ID=your-mcp-auth-server-id

# Agent service app credentials (for JWT bearer assertion)
OKTA_AGENT_ID=your-agent-service-app-id

# Agent private key (JSON format JWK for signing JWT)
# Generate in Okta Admin > Applications > Your Agent App > API Keys
OKTA_AGENT_PRIVATE_KEY={"kty":"RSA","kid":"your-key-id","use":"sig","n":"...","e":"AQAB","d":"..."}

# Audience for MCP resource server
OKTA_MCP_AUDIENCE=https://your-mcp-resource-server

# ============================================================================
# SECTION 4: AUTH0 - Token Vault (External APIs)
# ============================================================================
# Auth0 Token Vault for securely storing and retrieving tokens for:
# - Google Calendar
# - Salesforce (Phase 2)

# Auth0 tenant domain
AUTH0_DOMAIN=your-tenant.us.auth0.com

# Machine-to-machine app credentials
AUTH0_CLIENT_ID=your-auth0-m2m-client-id
AUTH0_CLIENT_SECRET=your-auth0-m2m-client-secret

# Token Vault API audience
AUTH0_VAULT_AUDIENCE=https://your-tenant.us.auth0.com/api/v2/

# Token Vault user profile ID (for linked accounts)
AUTH0_TOKEN_VAULT_PROFILE=tep_xxxxxxxxxxxx

# ============================================================================
# SECTION 5: INTERNAL MCP SERVER
# ============================================================================
# URL if using remote MCP server (optional - can run embedded)

MCP_SERVER_URL=https://your-mcp-server.onrender.com

# ============================================================================
# SECTION 6: FRONTEND
# ============================================================================
# Frontend URL for CORS

FRONTEND_URL=https://your-frontend.vercel.app

# Additional allowed origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# ============================================================================
# SECTION 7: DEPLOYMENT
# ============================================================================

# Environment (development, staging, production)
ENVIRONMENT=development

# Log level
LOG_LEVEL=INFO



